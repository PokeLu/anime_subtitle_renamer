[pytest]
testpaths = test
pythonpath = .
# 默认运行时跳过会真实调用大模型 API 的测试（联网、慢、消耗额度）
# 默认套件：python -m pytest ；包含 AI 集成测试：python -m pytest -m ai
addopts = -m "not ai"
markers =
    ai: 会真实调用大模型 API 的集成测试（联网、慢、消耗额度；运行方式：pytest -m ai）
